package client

// Create a new Column|func boards_ () {|| // POST /projects/{project_id}/boards/columns.json|| } ||
// List Columns|func boards_ () {|| // GET /projects/{project_id}/boards/columns.json|| } ||
// Delete a Column|func boards_ () {|| // DELETE  /boards/columns/{column_id}.json|| } ||
// Add a Task from the Backlog to a Column|func boards_ () {|| // POST /boards/columns/{column_id}/cards.json|| } ||
// List Cards in a Column|func boards_ () {|| // GET /boards/columns/{column_id}/cards.json|| } ||
// Move a Card|func boards_ () {|| // PUT /boards/columns/cards/{card_id}/move.json|| } ||
// Edit a Card|func boards_ () {|| // PUT /boards/columns/cards/{card_id}.json|| } ||
// Remove a Card|func boards_ () {|| // DELETE  /boards/columns/cards/{card_id}.json|| } ||

